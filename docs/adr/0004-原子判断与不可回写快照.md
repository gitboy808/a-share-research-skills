---
status: accepted
---

# 原子判断绑定不可回写的信息快照

为区分市场、产业、主题和个股各层错误，判断日志只接受单一可证伪命题，多层推理通过判断链连接；每个版本绑定形成时的信息快照，后来信息只能追加新版本。相比复合叙述和盘后重写，这会增加条目数量，但能消除“部分正确”、数据倒灌与事后改口。

## Consequences

- 新判断采用 `JYYYYMMDD-NNN`，旧短 ID 仅作来源别名。
- 结果状态与过程状态分开；过程失真样本不参与概率校准。
- 对象档案是可更新视图，判断日志始终是不可覆盖的事实源。
