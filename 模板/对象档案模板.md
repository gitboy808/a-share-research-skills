---
schema_version: "a-share-workspace-v3"
artifact_type: "object_dossier"
id: "DOS-KIND-IDENTIFIER"
status: "active"
created_at: "YYYY-MM-DDTHH:mm:ss+08:00"
information_cutoff: "YYYY-MM-DDTHH:mm:ss+08:00"
record_kind: "object_dossier"
objects: "KIND:OBJECT"
field_authority: "facts:investigate;analysis:analyze"
authority: "dossier_current_view"
source_refs: "atom:EVI-YYYYMMDD-NNN#001"
---

# 对象名称（代码，如适用）

> Schema：`a-share-workspace-v3`
> 类型：产业链 / 交易主题 / 个股
> 首次建档 / 最后核实 / 下次检查：
> 状态：有效 / 部分过期 / 待刷新 / 已结束

## 字段原子

每个当前字段独立版本化。事实字段只由调研工作流追加，分析字段只由分析工作流追加；更新旧字段时新增版本并填写 `替代`，不得改写旧原子。

### FIELD FIELD-IDENTITY v1

- **字段身份**：FIELD-IDENTITY
- **字段版本**：1
- **字段类型 / 写入阶段**：fact / investigate；或 analysis / analyze
- **字段状态**：active / limited / invalidated / ended / retired
- **字段值**：FIELD-VALUE
- **最后核实时间**：YYYY-MM-DDTHH:mm:ss+08:00
- **有效至 / 下次复核**：YYYY-MM-DDTHH:mm:ss+08:00
- **事件失效时间**：无；若触发则填写 YYYY-MM-DDTHH:mm:ss+08:00
- **来源引用**：atom:EVI-YYYYMMDD-NNN#001；分析字段还可引用 atom:JYYYYMMDD-NNN v1
- **替代**：none；后续版本填写 DOS-KIND-IDENTIFIER#FIELD-IDENTITY@N

文件级 `status` 只描述容器，不替代字段状态或字段时效；某字段过期不使其他字段同时退出当前视图。

## 当前观察与判断

| 类型 | ID/版本 | 命题或触发 | 到期 | 流水链接 |
|---|---|---|---|---|
| 观察候选 / 原子判断 |  |  |  |  |

## 跟踪指标（最多 6 个）

| 类别 | 指标 | 当前值/时点 | 触发值 | 来源 | 频率 | 状态 |
|---|---|---|---|---|---|---|

## 跨市场与轮动映射

| 类型 | 对端对象 | 机制/层级 | 状态与资金环境 | 时滞/窗口 | 证据簇 | 状态 |
|---|---|---|---|---|---|---|

## 历史版本

按日期链接证据包、判断、复盘和报告，不复制原文。旧 schema 缺失字段写“当时未记录”，禁止事后推断回填；旧文件级档案在迁移成字段原子前只作审计历史，不自动成为 `prospective_current` 输入。
