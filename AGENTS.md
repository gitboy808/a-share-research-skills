# A 股投研工作区启动协议 v3

## 路由

- A 股、大盘、产业链、交易主题、个股、外围传导、盘前、竞价、盘中、事件、复盘、元复盘或机会扫描请求，读取 `.agents/skills/a-share/a-share-research/SKILL.md` 路由。
- 显式调用 `a-share-scan`、`a-share-investigate`、`a-share-analyze`、`a-share-review` 或 `a-share-meta-review` 时，完整读取对应 `SKILL.md` 并执行共享阶段协议。
- “看看、研究一下、了解一下”等模糊请求只进入事实调研；投研无关请求不读取或写入投研状态。

## 强护栏

- `研究规则.md` 是规范规则唯一事实源，`CONTEXT.md` 定义领域语言，`docs/architecture.md` 定义当前 module、interface、写入所有权和阶段交接。
- 风险优先并允许正式弃权。关键证据不足、冲突未解或无法复验时停止方向判断，不为覆盖率强行预测。
- 工作流只消费 `.agents/skills/a-share/shared/context/` 的 `assemble` / `hydrate` 结果；版本化任务契约定义证据底线，语义候选不能替代必需覆盖。
- 正式判断、证据、复盘和信息快照只追加，不回写；未来信息不能改善旧判断的过程评价。结果轴与过程轴分别复盘。
- 事实、叙事与竞争假设分离；“主力意图、洗盘、出货、媒体诱多”不能写成事实。方向逻辑与价格纪律分离。
- 输出为研究判断和条件纪律，每篇投研报告注明“不构成投资建议”；不给具体买卖指令或仓位比例，不连接或执行自动交易。
- 用户指定的任一 A 股可研究；系统主动个股候选只限沪深主板 `60xxxx`、`000/001/002/003`。
- 入口只编排；扫描、调研、分析、复盘和元复盘严格遵守 `docs/architecture.md` 的写入所有权。L3 风险原则、证据底线、输出边界、错误分类、skill 架构和字段权限只能提案并等待用户确认。
