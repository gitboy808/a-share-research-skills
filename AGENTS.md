# A 股投研工作区启动协议 v3

本目录是跨会话持续的 A 股投研工作区。领域语言以 `CONTEXT.md` 为准，规范知识以 `研究规则.md` 为准，完整 skill 架构见 `docs/architecture.md`。

## 入口

- 用户提出 A 股、大盘、产业链、交易主题、个股、外围传导、盘前、竞价、盘中、事件、复盘、元复盘或机会扫描等投研请求时，使用 `.agents/skills/a-share/a-share-research/SKILL.md` 路由。
- 用户显式调用 `a-share-scan`、`a-share-investigate`、`a-share-analyze`、`a-share-review` 或 `a-share-meta-review` 时，读取对应 skill 并执行其共享预检。
- 模糊的“看看、研究一下、了解一下”默认只做事实调研，不自动形成判断。
- 投研无关请求不读取或写入投研常驻层、归档层、对象档案和策略库。

## L3 风险护栏

- 风险优先并允许弃权。证据不足时输出“不操作/暂不判断”；不得为覆盖率强行预测。
- 信息快照不可回写；未来信息不得用于美化旧判断过程。
- 事实、叙事与竞争假设分离。不得编造，不把“主力意图、洗盘、出货、媒体诱多”写成事实。
- 方向逻辑与价格纪律分离。上涨逻辑成立不等于当前价格仍具研究赔率。
- 结果轴与过程轴分别复盘，不得用部分正确合并原子命题。
- 输出为研究判断和条件纪律，不给具体交易指令或仓位比例；每篇投研报告注明“不构成投资建议”。
- 用户指定的任一 A 股可研究；系统主动个股候选只限沪深主板 `60xxxx`、`000/001/002/003`。

## 事实源与写入

- 工作区版本化文档是唯一事实源；校验脚本只检查 schema、ID、引用、版本链和派生索引。
- 入口只编排；扫描写观察候选；调研写证据与事实字段；分析写新判断与分析字段；复盘追加验证；元复盘在 L0–L2 内更新经验和策略。任何工作流不得越权回写历史。
- `CONTEXT.md`、`研究规则.md`、`经验库.md`、`当前判断.md`、`观察池.md`、`策略库/索引.md` 与相关对象档案按 skill 预检规则读取；不得默认通读全部归档。
- `模板/` 是唯一输出模板源；新产物使用 `a-share-workspace-v3`，旧 schema 仅供只读审计。

## 自治与运行边界

- L0 机械记录、L1 教训生命周期和授权范围内的 L2 参数演进可以自主执行；L3 风险原则、证据底线、输出边界、错误分类、skill 架构与字段权限只能提案并等待用户确认。
- 当前盘前、盘中、盘后、扫描与元复盘均由用户触发。保留未来调度接口，但不得连接或执行自动交易。
- 不主动执行任何 git 变更操作。
